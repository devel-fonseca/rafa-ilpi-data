version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: rafa-ilpi-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: rafa_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rafa_pass_prod}
      POSTGRES_DB: rafa_ilpi
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - rafa-ilpi-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rafa_user -d rafa_ilpi"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: rafa-ilpi-redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD:-redis_pass_prod}
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - rafa-ilpi-network
    healthcheck:
      test: ["CMD", "redis-cli", "--raw", "incr", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend NestJS API
  backend:
    image: rafa-ilpi-backend:latest
    container_name: rafa-ilpi-backend
    restart: unless-stopped
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
      target: production
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://rafa_user:${POSTGRES_PASSWORD:-rafa_pass_prod}@postgres:5432/rafa_ilpi
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-redis_pass_prod}
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production-please-use-env-file}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      COMPANY_NAME: ${COMPANY_NAME:-RAFA Labs}
      COMPANY_SUPPORT_EMAIL: ${COMPANY_SUPPORT_EMAIL:-suporte@rafalabs.com.br}
      ASAAS_API_KEY: ${ASAAS_API_KEY:-}
      ASAAS_WEBHOOK_TOKEN: ${ASAAS_WEBHOOK_TOKEN:-}
      ASAAS_ENVIRONMENT: ${ASAAS_ENVIRONMENT:-production}
      PRISMA_DB_CONNECTION_LIMIT: ${PRISMA_DB_CONNECTION_LIMIT:-10}
      PRISMA_DB_POOL_TIMEOUT_SECONDS: ${PRISMA_DB_POOL_TIMEOUT_SECONDS:-20}
      PRISMA_DB_CONNECT_TIMEOUT_SECONDS: ${PRISMA_DB_CONNECT_TIMEOUT_SECONDS:-10}
      PRISMA_DB_STATEMENT_TIMEOUT_MS: ${PRISMA_DB_STATEMENT_TIMEOUT_MS:-8000}
      PRISMA_DB_PGBOUNCER: ${PRISMA_DB_PGBOUNCER:-false}
      REQUEST_METRICS_MAX_EVENTS: ${REQUEST_METRICS_MAX_EVENTS:-50000}
      REQUEST_METRICS_DEFAULT_WINDOW_MINUTES: ${REQUEST_METRICS_DEFAULT_WINDOW_MINUTES:-15}
      REQUEST_METRICS_ALERT_P95_MS: ${REQUEST_METRICS_ALERT_P95_MS:-800}
      REQUEST_METRICS_ALERT_5XX_RATE_PERCENT: ${REQUEST_METRICS_ALERT_5XX_RATE_PERCENT:-0.5}
      ADMIN_DASHBOARD_OVERVIEW_CACHE_TTL_SECONDS: ${ADMIN_DASHBOARD_OVERVIEW_CACHE_TTL_SECONDS:-60}
    ports:
      - "3000:3000"
    volumes:
      - ./apps/backend/logs:/app/logs
      - backups_data:/backups
    networks:
      - rafa-ilpi-network
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3

  # Frontend React + Vite (Nginx)
  frontend:
    image: rafa-ilpi-frontend:latest
    container_name: rafa-ilpi-frontend
    restart: unless-stopped
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile
      target: production
      args:
        - VITE_API_URL=${VITE_API_URL:-/api}
    ports:
      - "8080:80"
    networks:
      - rafa-ilpi-network
    depends_on:
      - backend
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  backups_data:
    driver: local

networks:
  rafa-ilpi-network:
    driver: bridge
